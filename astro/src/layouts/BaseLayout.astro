---
const { title = 'Astro beta blog' } = Astro.props;
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <style>
      :root {
        color-scheme: light dark;
        --accent: #6c5ce7;
        --text: #f5f5f5;
        --bg: #0f172a;
        --panel: #111827;
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        font-family: system-ui, -apple-system, "Segoe UI", sans-serif;
        background: var(--bg);
        color: var(--text);
        line-height: 1.6;
      }

      a {
        color: #a5b4fc;
        text-decoration: none;
      }

      a:hover {
        text-decoration: underline;
      }

      header {
        background: linear-gradient(120deg, rgba(108, 92, 231, 0.9), rgba(79, 70, 229, 0.9));
        color: white;
        padding: 1rem 1.5rem;
        position: sticky;
        top: 0;
        z-index: 10;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.25);
      }

      .banner {
        display: flex;
        align-items: center;
        gap: 1rem;
        max-width: 960px;
        margin: 0 auto;
      }

      .badge {
        background: rgba(255, 255, 255, 0.16);
        color: white;
        padding: 0.3rem 0.75rem;
        border-radius: 999px;
        font-size: 0.85rem;
        letter-spacing: 0.02em;
      }

      .container {
        max-width: 960px;
        margin: 1.5rem auto 4rem;
        padding: 0 1.25rem;
      }

      footer {
        max-width: 960px;
        margin: 0 auto 2rem;
        padding: 0 1.25rem;
        color: #cbd5e1;
      }
    </style>
  </head>
  <body>
    <header>
      <div class="banner">
        <span class="badge">Astro beta</span>
        <div>
          <div style="font-weight: 700; font-size: 1.1rem">Experimental Astro blog</div>
          <div style="font-size: 0.95rem; color: rgba(255,255,255,0.85)">Preview served from /beta without touching the main site.</div>
        </div>
        <div style="margin-left: auto; font-size: 0.95rem;">
          <a href="/">Return to main site</a>
        </div>
      </div>
    </header>
    <main class="container">
      <slot />
    </main>
    <footer>
      <p>This is an Astro-powered beta that reads Markdown directly from <code>/posts</code>. Content edits remain single-sourced.</p>
    </footer>
  </body>
</html>
